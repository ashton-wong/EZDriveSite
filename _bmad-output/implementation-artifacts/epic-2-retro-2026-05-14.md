# Retrospective — Epic 2 (Partial)
Date: 2026-05-14
Status: Partial (some stories still in `review`)

Epic: 2 — Landing page content and preorder funnel (stories 2-1 → 2-4)

## Participants
- Ashton (Developer / story owner)
- Alice (Product Owner)
- Dana (QA)
- Amelia (Developer, facilitator)
- {user} (Project Lead)

## Epic Summary (partial)
- Total stories: 4 (2-1..2-4)
- Completed (story-level): 0 (all stories are `review` or `in-progress`)
- Primary focus: hero CTA & form wiring, benefit cards and how-it-works, A/B hooks, marketing copy placeholders
- Note: This is a partial retrospective — we'll revisit once all stories are `done`.

## What Went Well
- Hero Preorder CTA implemented and manually verified to scroll and focus the form; data attributes (`data-cta-id`, `data-ab-variant`) added (Story 2-1).
- A/B hooks and privacy-first analytics wiring implemented; `lib/analytics.js` extracts variant and includes it in `cta_click` events (Story 2-3).
- Benefit cards and how-it-works components implemented with semantic markup; tests were added for these components in this session (Story 2-2).
- Marketing copy extraction implemented via `content/marketing-copy.json` and documented (Story 2-4).

## Challenges Observed
- Testing inconsistency: several stories note manual QA required; test harness presence varies across stories (2-1 lacked JS tests, 2-2 added tests in-session). Automated coverage is uneven.
- QA/Manual-verify backlog: manual QA tasks remain for CTA analytics and A/B variant DebugView verification.
- Minor documentation gaps: `docs/editing-marketing-copy.md` exists but needs review by marketing to confirm workflow.
- Pending unit tests: CTA click behavior test remains a placeholder (no stable harness detected in 2-1).

## Key Insights
- Implementation is moving fast, but verification steps (automated tests, GA4 DebugView checks) are not uniformly applied — this creates a recurring manual QA burden.
- Data-attribute-driven experiments are in place; ensure marketing has a clear process to change `data-ab-variant` values and that analytics DebugView checks are owned.
- Content-driven approach (`marketing-copy.json`) will reduce developer friction, but the edit/preview workflow needs polish to be safe for non-dev editors.

## Action Items (specific, owner)
1. Add unit test for hero CTA click/focus behavior and integrate into CI if possible.
   - Owner: Ashton
   - Success: `__tests__/HeroCTA.test.jsx` exists and passes locally
2. Manual QA checklist and sign-off for A/B variant analytics (verify `variant` parameter appears in GA4 DebugView on `cta_click`).
   - Owner: Dana (QA)
   - Success: QA note indicating DebugView verification completed
3. Review and finalize `docs/editing-marketing-copy.md` with marketing; confirm fields and example edits.
   - Owner: Alice (PO) / Marketing
   - Success: Marketing confirms workflow and a sample edit is published
4. Promote test harness consistency: decide whether to keep Jest/RTL added in 2-2 and standardize on caret versions and setup.
   - Owner: Tech lead / Ashton
   - Success: `package.json` scripts include `test` and `__tests__/*` run in CI locally
5. Schedule full retrospective follow-up when remaining stories move to `done`.
   - Owner: Amelia (facilitator) / {user}
   - Success: Follow-up retro scheduled and communicated

## Pending Items / Dependencies
- CTA unit test (blocked until test harness decisions finalized)
- GA4 DebugView manual verification (requires staging or local debug env)
- Marketing confirmation for copy workflow

---
Partial retro saved. We'll update this document after completing remaining review items and QA.
